//! Tool registry — aggregates tool providers for the harness.
//!
//! Current implementation uses context-specific executors in `providers/` instead of
//! namespaced multi-provider registry. This module is reserved for future unification.
