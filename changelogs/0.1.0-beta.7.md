# 0.1.0-beta.7

## Fixes

- Fixed 400 error when switching from a model with thinking support to one without thinking support
  - Backend now validates model capabilities before adding thinking configuration to API requests
  - Frontend validates model thinking support before sending reasoning effort parameter
  - Prevents sending thinking-related parameters to models that don't support the feature
