# Changelog 0.1.0-alpha.11

## 🐛 Fixes

- **Critical Crash Fix**: Fixed app crash on startup when installed via Homebrew
  - Replaced `.unwrap()` calls with proper error handling in `AppState` initialization
  - Fixed binary name mismatch in production bundle - now correctly finds `uv` binary instead of architecture-specific name
  - Improved error handling for database lock acquisition and app data directory access

## 🔧 Technical Improvements

- **Error Handling**: Enhanced error handling in app initialization
  - `app_data_dir()` now returns proper `AppError::Tauri` instead of panicking
  - Database mutex lock errors are now properly handled with descriptive error messages
  - Binary path resolution now tries simple names first before architecture-specific names

- **Production Build**: Fixed bundled binary resolution for release builds
  - `get_bundled_uv_path()` now correctly finds binaries in production bundle
  - Added fallback to architecture-specific names for backwards compatibility
